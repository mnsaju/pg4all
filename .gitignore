__pycache__/
*.pyc
.venv/
.env
build_output/
.pytest_cache/

# Per-build superuser credentials and their encryption key — never commit.
secrets/
credentials/

# Reference screenshots dropped at repo root for design work, not app assets.
/image-*.png
